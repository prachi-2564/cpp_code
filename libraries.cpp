#include <iostream>		// for input/output
#include <iomanip>	/*The header <iomanip> is part of the Input/output library 
						of the C++ Standard Library. It defines the manipulator 
						functions resetiosflags() ,setiosflags() , setbase() , 
						setfill() , setprecision() , and setw() . */

#include <stdlib.h>	/*functions involving memory allocation, process control, 
						conversions and others. */
#include <string>		// for string funvtion
#include <conio.h>		/*Conio is an abbreviation for Console-Input-Output. This 
							file contains console input/output functions 
							used primarily by MS-DOS compilers.*/
#include <time.h>		/*contains time and date function declarations to 
							provide standardized access to time/date 
							manipulation and formatting.*/
#include <string.h>		/*string. h header file defines one variable type, 
							one macro, and many functions which can be used 
							to manipulate a string or array of characters.*/

